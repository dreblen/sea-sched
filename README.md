# Sea Sched

This project aims to provide a flexible, tag-based scheduling utility for event staffing. All data is stored in memory and/or locally.

## Getting Started

Run the following commands to start testing locally:

```shell
npm install
npm run dev
```

Run the following to generate a publishable build and preview it locally:
```shell
npm run build
npm run preview
```
